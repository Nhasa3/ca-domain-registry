A backend web application that simulates a real-world domain name registry system.
Build is in proces with Java, Spring Boot, PostgreSQL, and Thymeleaf — featuring domain registration, availability checking, renewal tracking, role-based access control, 
and a full admin management interface.
